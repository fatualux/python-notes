# Indexing

![Indexing](../../static/images/indexing.png)

Strings are zero-indexed, meaning that the first character in a string is at index `0`.
So, we can access a character in a string using its index:

```python
word = "Wolf"
word[0] # 'W'
word[3] # 'f'
```

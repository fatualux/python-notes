# Modules

In Python, a module is a file containing Python definitions (functions, classes, variables).

![Modules](../static/images/module.png)

Modules have .py.extension, x and they allow you to organize your Python code. They can be imported into other modules or scripts to reuse ther code.
